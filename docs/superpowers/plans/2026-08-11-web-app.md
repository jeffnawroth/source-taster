# Web Workspace (Web-Version) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Neue Vue-3-SPA `apps/web` unter `sourcetaster.com/app`, die den Kern-Workflow (Paste/PDF → Extract → Search → Match → Export) ohne Extension-Installation über die bestehende API `api.sourcetaster.com` anbietet.

**Architecture:** Eigenständige SPA (Vite + Vue 3 + Vuetify 3 + Pinia + vue-i18n), die ausschließlich die bestehende HTTP-API konsumiert (Verträge aus `@source-taster/types`). Deploy als statischer Docker-Service `web:8084`, geroutet via Caddy unter `sourcetaster.com/app/*` (Hash-History, kein Rewrite nötig).

**Tech Stack:** Vue 3, Vite, TypeScript, Vuetify 3, Pinia, vue-i18n, zod (via `@source-taster/types`), unpdf, Vitest. Node ≥22, pnpm 11.

## Global Constraints

- **Konsumiert nur bestehende API** — keine neuen Backend-Endpunkte; Verträge 1:1 aus `@source-taster/types` (Zod-Schemas als single source of truth).
- **`X-Client-Id: <UUIDv4>`** Header ist für `/api/extract`, `/api/search`, `/api/match`, `/api/user/*` Pflicht (sonst 401/400) — UUID einmalig generieren und in `localStorage` (Key `source-taster-client-id`) persistieren.
- **Brand-Farben** (aus ROADMAP.md): Primary `#4e2e92`, PrimaryLight `#6b4db8`, PrimaryDark `#3a1f6e`, Secondary `#1f6b7c`, SecondaryLight `#45a3b5`, Accent `#c9952e`, AccentLight `#e0b352`, Success `#2d7a31`, SuccessLight `#70c875`.
- **API-Base-URL** (Muster aus Extension `env.ts`): DEV → `http://localhost:8000`, PROD → `https://api.sourcetaster.com`. Prod-Build darf **kein** `localhost:8000` enthalten.
- **SPA-Routing**: `createWebHashHistory` mit base `'/app/'` — keine Server-Rewrites nötig.
- **i18n**: `de` und `en` (Locale-Keys wie Extension-Locales sinnvoll benennen).
- **Keine neuen Dependencies außer**: `vue`, `vue-router`, `pinia`, `vuetify`, `vue-i18n`, `@mdi/font`, `unpdf`, `zod`(transitiv via types), dev: `vite`, `@vitejs/plugin-vue`, `typescript`, `vue-tsc`, `vitest`, `eslint`, `@source-taster/eslint-config` (workspace).
- **Root-Workflow kompatibel**: `pnpm build`/`typecheck`/`lint`/`test` laufen als `pnpm run -r` über alle Workspace-Packages → `apps/web` muss alle 4 Scripts definieren und grün sein.
- Extension bleibt unverändert — keine Modifikationen an `apps/extension` oder `apps/api` in diesem Plan.

---

### Task 1: Scaffold `apps/web`

**Files:**
- Create: `apps/web/package.json`
- Create: `apps/web/tsconfig.json`
- Create: `apps/web/vite.config.ts`
- Create: `apps/web/index.html`
- Create: `apps/web/src/env.ts`
- Create: `apps/web/src/main.ts`
- Create: `apps/web/src/App.vue`
- Create: `apps/web/src/router/index.ts`
- Create: `apps/web/src/vite-env.d.ts`
- Modify: `apps/extension/tsconfig.json` ist NICHT zu ändern; `package.json` (root) bleibt unverändert

**Interfaces:**
- Consumes: nichts (frische App)
- Produces: `apps/web` als lauffähige Vite-App; `src/env.ts` exportiert `apiBaseUrl: string` (const, gerollt: `import.meta.env.DEV ? 'http://localhost:8000' : 'https://api.sourcetaster.com'`); Router-Instanz; `main.ts` mountet App.

- [ ] **Step 1: package.json schreiben**

```json
{
  "name": "@source-taster/web",
  "type": "module",
  "version": "2.1.22",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "lint": "eslint .",
    "lint:fix": "eslint --fix .",
    "typecheck": "vue-tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@mdi/font": "^7.4.47",
    "@source-taster/types": "workspace:^",
    "pinia": "^3.0.3",
    "unpdf": "^0.14.1",
    "vue": "^3.5.17",
    "vue-i18n": "^11.1.8",
    "vue-router": "^4.5.1",
    "vuetify": "^3.8.4"
  },
  "devDependencies": {
    "@source-taster/eslint-config": "workspace:^",
    "@vitejs/plugin-vue": "^6.0.0",
    "eslint": "^9.39.5",
    "typescript": "^5.9.3",
    "vite": "^7.0.0",
    "vitest": "^3.2.4",
    "vue-tsc": "^3.0.1"
  }
}
```

- [ ] **Step 2: tsconfig.json schreiben**

```json
{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "jsx": "preserve",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "noEmit": true,
    "types": ["vite/client"]
  },
  "include": ["src", "vite.config.ts"]
}
```

Hinweis: Root-`tsconfig.json` (Monorepo) bereits vorhanden — nur erweitern, `baseUrl`/`paths` aus Root gelten.

- [ ] **Step 3: vite.config.ts mit Dev-Proxy + Vitest schreiben**

```ts
import { fileURLToPath, URL } from 'node:url'
import vue from '@vitejs/plugin-vue'
import { defineConfig } from 'vitest/config'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url)),
    },
  },
  server: {
    port: 5173,
    proxy: {
      '/api': 'http://localhost:8000',
    },
  },
  test: {
    environment: 'node',
    globals: true,
  },
})
```

- [ ] **Step 4: index.html, env.ts, vite-env.d.ts schreiben**

`index.html` (Root der App; `lang="en"`, Title "Source Taster Web — Verify your references"):

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="https://sourcetaster.com/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Verify academic references online: extract citations, search 5 databases, detect AI-hallucinated sources." />
    <title>Source Taster — Verify your references</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>
```

`src/env.ts`:

```ts
export const apiBaseUrl = import.meta.env.DEV
  ? 'http://localhost:8000'
  : 'https://api.sourcetaster.com'
```

`src/vite-env.d.ts`:

```ts
/// <reference types="vite/client" />
```

- [ ] **Step 5: main.ts, App.vue, Router schreiben**

`src/main.ts` — Vuetify-Theme mit Brand-Farben (light + dark), i18n de/en, Pinia, Router:

```ts
import '@mdi/font/css/materialdesignicons.css'
import 'vuetify/styles'
import { createPinia } from 'pinia'
import { createApp } from 'vue'
import { createI18n } from 'vue-i18n'
import { createVuetify } from 'vuetify'
import App from './App.vue'
import { router } from './router'
import de from './locales/de.json'
import en from './locales/en.json'

const i18n = createI18n({
  legacy: false,
  locale: 'en',
  fallbackLocale: 'en',
  messages: { de, en },
})

const vuetify = createVuetify({
  theme: {
    defaultTheme: 'light',
    themes: {
      light: {
        dark: false,
        colors: {
          primary: '#4e2e92',
          'primary-light': '#6b4db8',
          'primary-dark': '#3a1f6e',
          secondary: '#1f6b7c',
          'secondary-light': '#45a3b5',
          accent: '#c9952e',
          'accent-light': '#e0b352',
          success: '#2d7a31',
          'success-light': '#70c875',
        },
      },
      dark: {
        dark: true,
        colors: {
          primary: '#6b4db8',
          secondary: '#45a3b5',
          accent: '#e0b352',
          success: '#70c875',
        },
      },
    },
  },
})

createApp(App).use(createPinia()).use(router).use(i18n).use(vuetify).mount('#app')
```

`src/router/index.ts` (Hash-History, base `/app/`):

```ts
import { createRouter, createWebHashHistory } from 'vue-router'
import HomeView from '@/views/HomeView.vue'

export const router = createRouter({
  history: createWebHashHistory('/app/'),
  routes: [
    { path: '/', name: 'home', component: HomeView },
    { path: '/results', name: 'results', component: () => import('@/views/ResultsView.vue') },
    { path: '/about', name: 'about', component: () => import('@/views/AboutView.vue') },
  ],
})
```

Hinweis: `HomeView.vue`, `ResultsView.vue`, `AboutView.vue` werden in Task 5 erstellt; bis dahin legt Task 5 sie an — Router-Import muss beim Typecheck existieren. Daher: Views in Task 5 VOR dem Typecheck-Lauf anlegen (Reihenfolge unten einhalten).

`src/App.vue` (Minimal-Shell, Header mit Brand-Logo + Nav, Vuetify-Layout; Task 6 poliert):

```vue
<template>
  <v-app>
    <v-app-bar app color="primary" dark>
      <v-app-bar-title>
        <router-link to="/" class="text-white text-decoration-none">Source Taster</router-link>
      </v-app-bar-title>
      <v-btn to="/about" text>About</v-btn>
    </v-app-bar>
    <v-main>
      <v-container fluid class="pa-6">
        <router-view />
      </v-container>
    </v-main>
  </v-app>
</template>

<script setup lang="ts">
</script>
```

- [ ] **Step 6: Locale-Dateien anlegen (minimal; Task 6 erweitert)**

`src/locales/en.json`:

```json
{
  "app": { "title": "Source Taster", "tagline": "Verify your references" },
  "nav": { "about": "About", "results": "Results" }
}
```

`src/locales/de.json`:

```json
{
  "app": { "title": "Source Taster", "tagline": "Prüfe deine Referenzen" },
  "nav": { "about": "Über", "results": "Ergebnisse" }
}
```

- [ ] **Step 7: Dummy-Views anlegen (damit Router + Typecheck grün)**

`src/views/HomeView.vue`, `src/views/ResultsView.vue`, `src/views/AboutView.vue` — je:

```vue
<template>
  <div>{{ $t('app.tagline') }}</div>
</template>

<script setup lang="ts">
</script>
```

(Alle drei identisch im Minimalzustand; Task 5 ersetzt sie vollständig.)

- [ ] **Step 8: Installieren, bauen, verifizieren**

Run: `pnpm install`
Run: `pnpm --filter @source-taster/web typecheck` → Expected: PASS (keine Fehler)
Run: `pnpm --filter @source-taster/web build` → Expected: PASS, `dist/` entsteht
Run: `pnpm --filter @source-taster/web lint` → Expected: PASS

- [ ] **Step 9: Commit**

```bash
git add apps/web
git commit -m "feat(web): scaffold vite vue app for web workspace"
```

---

### Task 2: API-Client + Client-ID

**Files:**
- Create: `apps/web/src/services/clientId.ts`
- Create: `apps/web/src/services/apiClient.ts`
- Test: `apps/web/src/services/apiClient.test.ts`

**Interfaces:**
- Consumes: `apiBaseUrl` aus `src/env.ts` (Task 1)
- Produces:
  - `getClientId(): string` — liest/schreibt `localStorage['source-taster-client-id']`, generiert UUIDv4 einmalig (crypto.randomUUID, Fallback wenn unavailable)
  - `apiClient(path: string, init?: RequestInit): Promise<unknown>` — fetch auf `${apiBaseUrl}${path}`; setzt Header `Content-Type: application/json` (wenn body), `X-Client-Id`; wirft bei `!res.ok` einen `ApiError` mit Status + `error`-Feld aus `ApiResponse`-Body; gibt bei Erfolg das `data`-Feld der `ApiResponse`-Envelope zurück.
  - `class ApiError extends Error { status: number; code?: string }`

- [ ] **Step 1: Failing Tests schreiben**

`src/services/apiClient.test.ts`:

```ts
import { describe, expect, it, vi, beforeEach, afterEach } from 'vitest'
import { ApiError, apiClient } from './apiClient'
import { getClientId } from './clientId'

const okResponse = (data: unknown) =>
  new Response(JSON.stringify({ success: true, data }), { status: 200, headers: { 'Content-Type': 'application/json' } })
const errorResponse = (error: string, status = 400) =>
  new Response(JSON.stringify({ success: false, error }), { status, headers: { 'Content-Type': 'application/json' } })

describe('getClientId', () => {
  beforeEach(() => localStorage.clear())
  it('generates a UUIDv4 and persists it', () => {
    const id = getClientId()
    expect(id).toMatch(/^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/)
    expect(localStorage.getItem('source-taster-client-id')).toBe(id)
  })
  it('returns the same id on second call', () => {
    const a = getClientId()
    const b = getClientId()
    expect(a).toBe(b)
  })
})

describe('apiClient', () => {
  beforeEach(() => localStorage.clear())
  afterEach(() => vi.unstubAllGlobals())

  it('sends X-Client-Id and returns data envelope', async () => {
    const fetchMock = vi.fn().mockResolvedValue(okResponse({ ok: true }))
    vi.stubGlobal('fetch', fetchMock)
    const id = getClientId()
    const result = await apiClient('/api/extract', { method: 'POST', body: JSON.stringify({ text: 'x' }) })
    expect(result).toEqual({ ok: true })
    const [url, init] = fetchMock.mock.calls[0]
    expect(String(url)).toBe('http://localhost:8000/api/extract')
    expect((init!.headers as Record<string, string>)['X-Client-Id']).toBe(id)
  })

  it('throws ApiError with status and message on error envelope', async () => {
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue(errorResponse('Invalid reference', 400)))
    await expect(apiClient('/api/match')).rejects.toMatchObject({ status: 400, message: 'Invalid reference' })
  })

  it('falls back to HTTP status text when body is not an envelope', async () => {
    vi.stubGlobal('fetch', vi.fn().mockResolvedValue(new Response('oops', { status: 500 })))
    await expect(apiClient('/api/search')).rejects.toBeInstanceOf(ApiError)
  })
})
```

- [ ] **Step 2: Tests laufen lassen (erwartetes FAIL)**

Run: `pnpm --filter @source-taster/web test`
Expected: FAIL — Module `./apiClient`/`./clientId` existieren nicht.

- [ ] **Step 3: clientId.ts implementieren**

```ts
const STORAGE_KEY = 'source-taster-client-id'

function generateUuid(): string {
  if (typeof crypto !== 'undefined' && typeof crypto.randomUUID === 'function') {
    return crypto.randomUUID()
  }
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, (c) => {
    const r = (Math.random() * 16) | 0
    const v = c === 'x' ? r : (r & 0x3) | 0x8
    return v.toString(16)
  })
}

export function getClientId(): string {
  const existing = localStorage.getItem(STORAGE_KEY)
  if (existing) return existing
  const id = generateUuid()
  localStorage.setItem(STORAGE_KEY, id)
  return id
}
```

- [ ] **Step 4: apiClient.ts implementieren**

```ts
import { apiBaseUrl } from '@/env'
import { getClientId } from './clientId'

export class ApiError extends Error {
  constructor(
    public readonly status: number,
    message: string,
  ) {
    super(message)
    this.name = 'ApiError'
  }
}

export async function apiClient(path: string, init: RequestInit = {}): Promise<unknown> {
  const headers = new Headers(init.headers)
  headers.set('X-Client-Id', getClientId())
  if (init.body) headers.set('Content-Type', 'application/json')

  const res = await fetch(`${apiBaseUrl}${path}`, { ...init, headers })

  if (!res.ok) {
    let message = res.statusText
    try {
      const body = (await res.json()) as { success?: boolean; error?: string }
      if (body?.error) message = body.error
    }
    catch {
      // non-JSON error body — statusText behalten
    }
    throw new ApiError(res.status, message)
  }

  const body = (await res.json()) as { success: boolean; data?: unknown }
  return body.data
}
```

- [ ] **Step 5: Tests laufen lassen (erwartetes PASS)**

Run: `pnpm --filter @source-taster/web test`
Expected: PASS (3 describe-Blöcke grün)

- [ ] **Step 6: Commit**

```bash
git add apps/web/src/services
git commit -m "feat(web): api client with client id header"
```

---

### Task 3: Services (extraction, search, matching, ai-secrets)

**Files:**
- Create: `apps/web/src/services/extractionService.ts`
- Create: `apps/web/src/services/searchService.ts`
- Create: `apps/web/src/services/matchingService.ts`
- Create: `apps/web/src/services/aiSecretsService.ts`
- Test: `apps/web/src/services/services.test.ts`

**Interfaces:**
- Consumes: `apiClient` (Task 2); Schemas aus `@source-taster/types`
- Produces:
  - `extractReferences(text: string, aiSettings?: ApiAISettings): Promise<ApiExtractData>` — POST `/api/extract` mit `{ text, aiSettings }` (aiSettings optional)
  - `searchReferences(references: ApiSearchReference[]): Promise<ApiSearchData>` — POST `/api/search` mit `{ references }`
  - `matchReference(reference: ApiMatchReference, candidates: ApiMatchCandidate[]): Promise<ApiMatchData>` — POST `/api/match` mit `{ reference, candidates }`
  - `saveAiSecret(provider: ApiAIProvider, apiKey: string): Promise<unknown>` — POST `/api/user/ai-secrets`
  - `getAiSecretInfo(): Promise<{ hasApiKey: boolean; provider?: ApiAIProvider }>` — GET `/api/user/ai-secrets`
  - `deleteAiSecret(): Promise<unknown>` — DELETE `/api/user/ai-secrets`
- Wichtige Typen (aus `@source-taster/types`): `ApiExtractData`, `ApiSearchReference`, `ApiSearchData`, `ApiMatchReference`, `ApiMatchCandidate`, `ApiMatchData`, `ApiAISettings`, `ApiAIProvider`

- [ ] **Step 1: Failing Tests schreiben**

`src/services/services.test.ts`:

```ts
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'
import { apiClient } from './apiClient'
import { deleteAiSecret, extractReferences, getAiSecretInfo, matchReference, saveAiSecret, searchReferences } from './extractionService'
```

Hinweis: Services liegen in separaten Dateien; der Test importiert aus den einzelnen Dateien:

```ts
import { extractReferences } from './extractionService'
import { searchReferences } from './searchService'
import { matchReference } from './matchingService'
import { saveAiSecret, getAiSecretInfo, deleteAiSecret } from './aiSecretsService'
import { apiClient } from './apiClient'

vi.mock('./apiClient', () => ({
  apiClient: vi.fn(),
}))

const mocked = vi.mocked(apiClient)

describe('extractionService', () => {
  it('posts text and returns data', async () => {
    const data = { references: [] }
    mocked.mockResolvedValue(data as never)
    const result = await extractReferences('Some ref text', { provider: 'openai', model: 'gpt-5-mini' })
    expect(mocked).toHaveBeenCalledWith('/api/extract', {
      method: 'POST',
      body: JSON.stringify({ text: 'Some ref text', aiSettings: { provider: 'openai', model: 'gpt-5-mini' } }),
    })
    expect(result).toEqual(data)
  })
  it('works without aiSettings', async () => {
    mocked.mockResolvedValue({ references: [] } as never)
    await extractReferences('text')
    expect(mocked).toHaveBeenCalledWith('/api/extract', { method: 'POST', body: JSON.stringify({ text: 'text' }) })
  })
})

describe('searchService', () => {
  it('posts references array', async () => {
    mocked.mockResolvedValue({ results: [] } as never)
    await searchReferences([{ id: '11111111-1111-4111-8111-111111111111', metadata: {} } as never])
    expect(mocked).toHaveBeenCalledWith('/api/search', {
      method: 'POST',
      body: JSON.stringify({ references: [{ id: '11111111-1111-4111-8111-111111111111', metadata: {} }] }),
    })
  })
})

describe('matchingService', () => {
  it('posts reference and candidates', async () => {
    mocked.mockResolvedValue({ evaluations: [] } as never)
    await matchReference({ id: '11111111-1111-4111-8111-111111111111', metadata: {} } as never, [])
    expect(mocked).toHaveBeenCalledWith('/api/match', {
      method: 'POST',
      body: JSON.stringify({
        reference: { id: '11111111-1111-4111-8111-111111111111', metadata: {} },
        candidates: [],
      }),
    })
  })
})

describe('aiSecretsService', () => {
  it('saves, fetches info and deletes', async () => {
    mocked.mockResolvedValueOnce({ saved: true } as never)
    await saveAiSecret('openai', 'sk-test')
    expect(mocked).toHaveBeenCalledWith('/api/user/ai-secrets', {
      method: 'POST',
      body: JSON.stringify({ provider: 'openai', apiKey: 'sk-test' }),
    })

    mocked.mockResolvedValueOnce({ hasApiKey: true, provider: 'openai' } as never)
    await expect(getAiSecretInfo()).resolves.toEqual({ hasApiKey: true, provider: 'openai' })

    mocked.mockResolvedValueOnce({ deleted: true } as never)
    await deleteAiSecret()
    expect(mocked).toHaveBeenCalledWith('/api/user/ai-secrets', { method: 'DELETE' })
  })
})
```

- [ ] **Step 2: Tests laufen lassen (erwartetes FAIL)**

Run: `pnpm --filter @source-taster/web test`
Expected: FAIL — Services existieren nicht.

- [ ] **Step 3: extractionService.ts implementieren**

```ts
import type { ApiAISettings, ApiExtractData } from '@source-taster/types'
import { apiClient } from './apiClient'

export async function extractReferences(text: string, aiSettings?: ApiAISettings): Promise<ApiExtractData> {
  const body: Record<string, unknown> = { text }
  if (aiSettings) body.aiSettings = aiSettings
  return apiClient('/api/extract', { method: 'POST', body: JSON.stringify(body) }) as Promise<ApiExtractData>
}
```

- [ ] **Step 4: searchService.ts implementieren**

```ts
import type { ApiSearchData, ApiSearchReference } from '@source-taster/types'
import { apiClient } from './apiClient'

export async function searchReferences(references: ApiSearchReference[]): Promise<ApiSearchData> {
  return apiClient('/api/search', { method: 'POST', body: JSON.stringify({ references }) }) as Promise<ApiSearchData>
}
```

- [ ] **Step 5: matchingService.ts implementieren**

```ts
import type { ApiMatchCandidate, ApiMatchData, ApiMatchReference } from '@source-taster/types'
import { apiClient } from './apiClient'

export async function matchReference(reference: ApiMatchReference, candidates: ApiMatchCandidate[]): Promise<ApiMatchData> {
  return apiClient('/api/match', { method: 'POST', body: JSON.stringify({ reference, candidates }) }) as Promise<ApiMatchData>
}
```

- [ ] **Step 6: aiSecretsService.ts implementieren**

```ts
import type { ApiAIProvider } from '@source-taster/types'
import { apiClient } from './apiClient'

export async function saveAiSecret(provider: ApiAIProvider, apiKey: string): Promise<unknown> {
  return apiClient('/api/user/ai-secrets', { method: 'POST', body: JSON.stringify({ provider, apiKey }) })
}

export async function getAiSecretInfo(): Promise<{ hasApiKey: boolean; provider?: ApiAIProvider }> {
  return apiClient('/api/user/ai-secrets') as Promise<{ hasApiKey: boolean; provider?: ApiAIProvider }>
}

export async function deleteAiSecret(): Promise<unknown> {
  return apiClient('/api/user/ai-secrets', { method: 'DELETE' })
}
```

- [ ] **Step 7: Tests laufen lassen (erwartetes PASS)**

Run: `pnpm --filter @source-taster/web test`
Expected: PASS

- [ ] **Step 8: Commit**

```bash
git add apps/web/src/services
git commit -m "feat(web): api service wrappers for extract, search, match, ai-secrets"
```

---

### Task 4: Stores (extraction, verification, aiSettings)

**Files:**
- Create: `apps/web/src/stores/extraction.ts`
- Create: `apps/web/src/stores/verification.ts`
- Create: `apps/web/src/stores/aiSettings.ts`
- Test: `apps/web/src/stores/stores.test.ts`

**Interfaces:**
- Consumes: Services aus Task 3; Typen `ApiExtractReference`, `ApiSearchResult`, `ApiMatchEvaluation` aus `@source-taster/types`
- Produces:
  - `useExtractionStore`: state `references: ApiExtractReference[]`, `loading: boolean`, `error: string | null`; actions `extract(text, aiSettings?)` (setzt refs), `removeReference(id: string)`
  - `useVerificationStore`: state `results: Record<string, { bestScore: number | null; status: 'verified' | 'partial' | 'not-found' | 'pending' }>`, `running: boolean`, `error: string | null`; action `verifyAll(references: ApiExtractReference[])` — ruft `searchReferences` (Batch) und `matchReference` je Referenz auf, klassifiziert per `classifyScore` (≥85 verified, 50–84 partial, sonst/keine Kandidaten not-found)
  - `useAiSettingsStore`: state `provider: ApiAIProvider`, `model: ApiAIModel`, `hasApiKey: boolean | null`; actions `loadInfo()`, `save(apiKey)`, `remove()`
  - `classifyScore(score: number | null): 'verified' | 'partial' | 'not-found'` in `src/utils/scores.ts` (siehe Task 5, hier nur als Import genutzt — deshalb: Task 5 `scores.ts` MUSS vor dem Typecheck stehen; Reihenfolge der Tasks einhalten oder `scores.ts` hier gleich mit anlegen)

- [ ] **Step 1: scores.ts zuerst anlegen (damit Stores kompilieren)**

`src/utils/scores.ts`:

```ts
export type VerificationStatus = 'verified' | 'partial' | 'not-found'

export function classifyScore(score: number | null): VerificationStatus {
  if (score === null) return 'not-found'
  if (score >= 85) return 'verified'
  if (score >= 50) return 'partial'
  return 'not-found'
}

export function bestScoreOf(evaluations: { matchDetails: { overallScore: number } }[]): number | null {
  if (evaluations.length === 0) return null
  return Math.max(...evaluations.map(e => e.matchDetails.overallScore))
}
```

- [ ] **Step 2: Failing Tests schreiben**

`src/stores/stores.test.ts`:

```ts
import { beforeEach, describe, expect, it, vi } from 'vitest'
import { createPinia, setActivePinia } from 'pinia'
import { useExtractionStore } from './extraction'
import { useVerificationStore } from './verification'
import { classifyScore } from '@/utils/scores'

vi.mock('../services/extractionService', () => ({ extractReferences: vi.fn() }))
vi.mock('../services/searchService', () => ({ searchReferences: vi.fn() }))
vi.mock('../services/matchingService', () => ({ matchReference: vi.fn() }))
vi.mock('../services/aiSecretsService', () => ({ getAiSecretInfo: vi.fn() }))

import { extractReferences } from '../services/extractionService'
import { searchReferences } from '../services/searchService'
import { matchReference } from '../services/matchingService'

const U = '11111111-1111-4111-8111-111111111111'
const ref = (id: string) => ({ id, originalText: 'x', metadata: { title: 'T' } })

describe('classifyScore', () => {
  it('classifies thresholds', () => {
    expect(classifyScore(null)).toBe('not-found')
    expect(classifyScore(0)).toBe('not-found')
    expect(classifyScore(60)).toBe('partial')
    expect(classifyScore(85)).toBe('verified')
  })
})

describe('useExtractionStore', () => {
  beforeEach(() => setActivePinia(createPinia()))
  it('extracts and stores references', async () => {
    vi.mocked(extractReferences).mockResolvedValue({ references: [ref(U)] } as never)
    const store = useExtractionStore()
    await store.extract('text')
    expect(store.references).toHaveLength(1)
    expect(store.loading).toBe(false)
    store.removeReference(U)
    expect(store.references).toHaveLength(0)
  })
})

describe('useVerificationStore', () => {
  beforeEach(() => setActivePinia(createPinia()))
  it('searches and matches each reference', async () => {
    vi.mocked(searchReferences).mockResolvedValue({
      results: [{ referenceId: U, candidates: [{ id: '22222222-2222-4222-8222-222222222222', source: 'crossref', metadata: {} }] }],
    } as never)
    vi.mocked(matchReference).mockResolvedValue({
      evaluations: [{ candidateId: '22222222-2222-4222-8222-222222222222', matchDetails: { fieldDetails: [], overallScore: 92 } }],
    } as never)

    const store = useVerificationStore()
    await store.verifyAll([ref(U)])
    expect(store.results[U].bestScore).toBe(92)
    expect(store.results[U].status).toBe('verified')
    expect(vi.mocked(matchReference)).toHaveBeenCalledTimes(1)
  })

  it('marks not-found when no candidates', async () => {
    vi.mocked(searchReferences).mockResolvedValue({ results: [{ referenceId: U, candidates: [] }] } as never)
    const store = useVerificationStore()
    await store.verifyAll([ref(U)])
    expect(store.results[U].status).toBe('not-found')
    expect(vi.mocked(matchReference)).not.toHaveBeenCalled()
  })
})
```

- [ ] **Step 3: Tests laufen lassen (erwartetes FAIL)**

Run: `pnpm --filter @source-taster/web test`
Expected: FAIL — Stores existieren nicht.

- [ ] **Step 4: extraction.ts implementieren**

```ts
import { defineStore } from 'pinia'
import type { ApiAISettings, ApiExtractReference } from '@source-taster/types'
import { extractReferences } from '@/services/extractionService'

export const useExtractionStore = defineStore('extraction', {
  state: () => ({
    references: [] as ApiExtractReference[],
    loading: false,
    error: null as string | null,
  }),
  actions: {
    async extract(text: string, aiSettings?: ApiAISettings) {
      this.loading = true
      this.error = null
      try {
        const data = await extractReferences(text, aiSettings)
        this.references = data.references
      }
      catch (e) {
        this.error = e instanceof Error ? e.message : 'Extraction failed'
      }
      finally {
        this.loading = false
      }
    },
    removeReference(id: string) {
      this.references = this.references.filter(r => r.id !== id)
    },
  },
})
```

- [ ] **Step 5: verification.ts implementieren**

```ts
import { defineStore } from 'pinia'
import type { ApiExtractReference, ApiMatchData } from '@source-taster/types'
import { matchReference } from '@/services/matchingService'
import { searchReferences } from '@/services/searchService'
import { bestScoreOf, classifyScore, type VerificationStatus } from '@/utils/scores'

export interface ReferenceResult {
  bestScore: number | null
  status: VerificationStatus
}

export const useVerificationStore = defineStore('verification', {
  state: () => ({
    results: {} as Record<string, ReferenceResult>,
    running: false,
    error: null as string | null,
  }),
  actions: {
    async verifyAll(references: ApiExtractReference[]) {
      this.running = true
      this.error = null
      this.results = {}
      try {
        const { results } = await searchReferences(references.map(r => ({ id: r.id, metadata: r.metadata })))
        for (const reference of references) {
          const candidates = results.find(res => res.referenceId === reference.id)?.candidates ?? []
          if (candidates.length === 0) {
            this.results[reference.id] = { bestScore: null, status: 'not-found' }
            continue
          }
          const data: ApiMatchData = await matchReference(
            { id: reference.id, metadata: reference.metadata },
            candidates.map(c => ({ id: c.id, metadata: c.metadata })),
          )
          const bestScore = bestScoreOf(data.evaluations)
          this.results[reference.id] = { bestScore, status: classifyScore(bestScore) }
        }
      }
      catch (e) {
        this.error = e instanceof Error ? e.message : 'Verification failed'
      }
      finally {
        this.running = false
      }
    },
  },
})
```

- [ ] **Step 6: aiSettings.ts implementieren**

```ts
import { defineStore } from 'pinia'
import type { ApiAIModel, ApiAIProvider } from '@source-taster/types'
import { getAiSecretInfo } from '@/services/aiSecretsService'

export const useAiSettingsStore = defineStore('aiSettings', {
  state: () => ({
    provider: 'openai' as ApiAIProvider,
    model: 'gpt-5-mini' as ApiAIModel,
    hasApiKey: null as boolean | null,
  }),
  actions: {
    async loadInfo() {
      try {
        const info = await getAiSecretInfo()
        this.hasApiKey = info.hasApiKey
        if (info.provider) this.provider = info.provider
      }
      catch {
        this.hasApiKey = false
      }
    },
  },
})
```

- [ ] **Step 7: Tests laufen lassen (erwartetes PASS)**

Run: `pnpm --filter @source-taster/web test`
Expected: PASS

- [ ] **Step 8: Typecheck + Lint**

Run: `pnpm --filter @source-taster/web typecheck` → PASS
Run: `pnpm --filter @source-taster/web lint` → PASS

- [ ] **Step 9: Commit**

```bash
git add apps/web/src/stores apps/web/src/utils
git commit -m "feat(web): pinia stores for extraction and verification"
```

---

### Task 5: Views + Komponenten (Home, Results, About)

**Files:**
- Create: `apps/web/src/views/HomeView.vue` (ersetzt Dummy)
- Create: `apps/web/src/views/ResultsView.vue` (ersetzt Dummy)
- Create: `apps/web/src/views/AboutView.vue` (ersetzt Dummy)
- Create: `apps/web/src/utils/pdfUtils.ts`
- Create: `apps/web/src/utils/bibtex.ts`
- Test: `apps/web/src/utils/bibtex.test.ts`
- Create: `apps/web/src/components/ScoreBadge.vue`
- Create: `apps/web/src/components/RefCard.vue`

**Interfaces:**
- Consumes: Stores aus Task 4; `apiClient`-Services; `unpdf`; Typen aus `@source-taster/types` (CSLItem, ApiExtractReference, ApiMatchEvaluation)
- Produces:
  - `extractTextFromPdfFile(file: File): Promise<string>` in `utils/pdfUtils.ts` (Kopie aus Extension, unpdf)
  - `cslToBibtex(item: CSLItem): string` in `utils/bibtex.ts`
  - Views: HomeView (Input-Flow), ResultsView (Ergebnis-Liste + Export), AboutView (Statik)

- [ ] **Step 1: Failing Test für bibtex schreiben**

`src/utils/bibtex.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import { cslToBibtex } from './bibtex'

describe('cslToBibtex', () => {
  it('renders a minimal article', () => {
    const item = {
      type: 'article-journal',
      title: 'Deep Learning',
      author: [{ family: 'LeCun', given: 'Yann' }],
      'container-title': 'Nature',
      issued: { 'date-parts': [[2015, 5, 28]] },
      volume: '521',
      page: '436–444',
      DOI: '10.1038/nature14539',
    }
    const out = cslToBibtex(item as never)
    expect(out).toContain('@article{')
    expect(out).toContain('title = {Deep Learning}')
    expect(out).toContain('author = {LeCun, Yann}')
    expect(out).toContain('doi = {10.1038/nature14539}')
  })
})
```

- [ ] **Step 2: Test laufen lassen (erwartetes FAIL)**

Run: `pnpm --filter @source-taster/web test`
Expected: FAIL — `bibtex.ts` existiert nicht.

- [ ] **Step 3: bibtex.ts implementieren**

```ts
import type { CSLItem } from '@source-taster/types'

function typeToBibtex(type: string): string {
  const map: Record<string, string> = {
    'article-journal': 'article',
    'article-magazine': 'article',
    'article-newspaper': 'article',
    book: 'book',
    chapter: 'incollection',
    'paper-conference': 'inproceedings',
    thesis: 'phdthesis',
    report: 'techreport',
    webpage: 'misc',
    software: 'software',
  }
  return map[type] ?? 'misc'
}

function authorsToBibtex(authors: { family?: string; given?: string }[]): string {
  return authors.map(a => `${a.family ?? ''}, ${a.given ?? ''}`.trim().replace(/, $/, '')).join(' and ')
}

function escape(value: string): string {
  return value.replace(/([{}#\\])/g, '\\$1').replace(/–/g, '--')
}

export function cslToBibtex(item: CSLItem): string {
  const lines: string[] = []
  const entryType = typeToBibtex(item.type ?? 'book')
  const key = item.id ?? `${item.author?.[0]?.family ?? 'unknown'}${item.issued?.['date-parts']?.[0]?.[0] ?? ''}`
  lines.push(`@${entryType}{${key},`)
  const push = (field: string, value: string | undefined) => {
    if (value) lines.push(`  ${field} = {${escape(value)}},`)
  }
  push('title', item.title)
  if (item.author?.length) push('author', authorsToBibtex(item.author as never))
  push('journal', item['container-title'])
  push('booktitle', item['container-title'])
  push('volume', item.volume)
  push('number', item.issue)
  push('pages', item.page)
  push('doi', item.DOI)
  push('url', item.URL)
  push('year', String(item.issued?.['date-parts']?.[0]?.[0] ?? ''))
  push('publisher', item.publisher)
  lines.push('}')
  return lines.join('\n')
}
```

- [ ] **Step 4: Test laufen lassen (erwartetes PASS)**

Run: `pnpm --filter @source-taster/web test`
Expected: PASS

- [ ] **Step 5: pdfUtils.ts anlegen (Kopie aus Extension, gleiche API)**

`src/utils/pdfUtils.ts`:

```ts
import { extractText, getDocumentProxy } from 'unpdf'

export async function extractTextFromPdfFile(file: File): Promise<string> {
  if (!file || file.type !== 'application/pdf') {
    console.warn('Invalid file type. Expected PDF, got:', file?.type)
    return ''
  }
  try {
    const buffer = await file.arrayBuffer()
    const pdf = await getDocumentProxy(new Uint8Array(buffer))
    const { text } = await extractText(pdf, { mergePages: true })
    return text.trim() || ''
  }
  catch (error) {
    console.error('Error extracting text from PDF:', error)
    return ''
  }
}
```

- [ ] **Step 6: HomeView.vue schreiben (ersetzt Dummy)**

Flow: Textarea (v-model `inputText`), `v-file-input` (accept="application/pdf", `@change` → `extractTextFromPdfFile` → `inputText`), optionaler "AI-Extraktion"-Schalter + Settings-Summary (Task 6 verfeinert), Button "Extract" → `extractionStore.extract(inputText, aiSettings wenn aktiv)` → `router.push('/results')`; Fehleranzeige `v-alert`; Ladeanzeige `v-progress-linear`.

```vue
<template>
  <v-row justify="center">
    <v-col cols="12" md="8">
      <h1 class="text-h4 mb-2">{{ $t('home.title') }}</h1>
      <p class="text-body-1 mb-6">{{ $t('home.subtitle') }}</p>

      <v-alert v-if="extraction.error" type="error" class="mb-4" closable @click:close="extraction.error = null">
        {{ extraction.error }}
      </v-alert>

      <v-textarea
        v-model="inputText"
        :label="$t('home.textareaLabel')"
        variant="outlined"
        auto-grow
        rows="8"
        class="mb-4"
      />

      <v-file-input
        :label="$t('home.pdfLabel')"
        accept="application/pdf"
        prepend-icon="mdi-file-pdf-box"
        class="mb-4"
        @change="onPdf"
      />

      <v-switch v-model="useAi" :label="$t('home.useAiLabel')" class="mb-2" />
      <div v-if="useAi" class="mb-4">
        <v-select v-model="aiSettings.provider" :items="providers" item-title="label" item-value="value" label="Provider" />
      </div>

      <v-btn
        color="primary"
        size="large"
        :loading="extraction.loading"
        :disabled="!inputText.trim()"
        @click="onExtract"
      >
        {{ $t('home.extractButton') }}
      </v-btn>
    </v-col>
  </v-row>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { useRouter } from 'vue-router'
import { useExtractionStore } from '@/stores/extraction'
import { extractTextFromPdfFile } from '@/utils/pdfUtils'

const router = useRouter()
const extraction = useExtractionStore()

const inputText = ref('')
const useAi = ref(false)
const aiSettings = ref({ provider: 'openai' })

const providers = [
  { label: 'OpenAI', value: 'openai' },
  { label: 'Anthropic (Claude)', value: 'anthropic' },
  { label: 'Google (Gemini)', value: 'google' },
  { label: 'DeepSeek', value: 'deepseek' },
]

async function onPdf(file: File | null) {
  if (!file) return
  const text = await extractTextFromPdfFile(file)
  if (text) inputText.value = text
}

async function onExtract() {
  await extraction.extract(inputText.value, useAi.value ? { provider: aiSettings.value.provider as never, model: 'gpt-5-mini' as never } : undefined)
  if (!extraction.error) router.push('/results')
}
</script>
```

- [ ] **Step 7: ResultsView.vue schreiben (ersetzt Dummy)**

Zeigt `extraction.references` als Karten (RefCard), Button "Verify" → `verification.verifyAll(references)`, dann pro Ref Status-Badge (ScoreBadge) + `v-expansion-panels` mit Feld-Details (bestMatch aus evaluations), Export-Menü (JSON + BibTeX Download), Fehler `v-alert`, Fortschritt `v-progress-linear` solange `verification.running`.

```vue
<template>
  <div>
    <v-row justify="center">
      <v-col cols="12" md="8">
        <div class="d-flex align-center mb-4">
          <v-btn variant="text" to="/" prepend-icon="mdi-arrow-left">{{ $t('results.back') }}</v-btn>
          <v-spacer />
          <v-btn color="primary" :loading="verification.running" :disabled="extraction.references.length === 0" @click="run">
            {{ $t('results.verifyButton') }}
          </v-btn>
          <v-menu>
            <template #activator="{ props }">
              <v-btn v-bind="props" variant="tonal" class="ml-2" prepend-icon="mdi-download">{{ $t('results.export') }}</v-btn>
            </template>
            <v-list>
              <v-list-item @click="exportJson"><v-list-item-title>JSON (CSL)</v-list-item-title></v-list-item>
              <v-list-item @click="exportBibtex"><v-list-item-title>BibTeX</v-list-item-title></v-list-item>
            </v-list>
          </v-menu>
        </div>

        <v-progress-linear v-if="verification.running" indeterminate color="primary" class="mb-4" />

        <v-alert v-if="verification.error" type="error" class="mb-4">{{ verification.error }}</v-alert>
        <v-alert v-else-if="!verification.running && !extraction.references.length" type="info" class="mb-4">
          {{ $t('results.empty') }}
        </v-alert>

        <v-expansion-panels v-for="ref in extraction.references" :key="ref.id" class="mb-2" variant="accordion">
          <v-expansion-panel>
            <v-expansion-panel-title>
              <div class="d-flex align-center w-100">
                <span class="text-subtitle-1 mr-4">{{ ref.metadata.title || ref.originalText.slice(0, 80) }}</span>
                <v-spacer />
                <ScoreBadge v-if="verification.results[ref.id]" :status="verification.results[ref.id].status" :score="verification.results[ref.id].bestScore" />
              </div>
            </v-expansion-panel-title>
            <v-expansion-panel-text>
              <p class="text-body-2 text-medium-emphasis mb-3">{{ ref.originalText }}</p>
              <template v-if="verification.results[ref.id] && verification.results[ref.id].status !== 'not-found'">
                <v-list v-for="evalItem in matches[ref.id] || []" :key="evalItem.candidateId" density="compact">
                  <v-list-item>
                    <v-list-item-title>{{ $t('results.match') }}: {{ evalItem.matchDetails.overallScore }}%</v-list-item-title>
                    <v-list-item-subtitle v-for="fd in evalItem.matchDetails.fieldDetails.slice(0, 5)" :key="fd.field">
                      {{ fd.field }}: {{ fd.fieldScore }}%
                    </v-list-item-subtitle>
                  </v-list-item>
                </v-list>
              </template>
              <v-alert v-else-if="verification.results[ref.id]?.status === 'not-found'" type="warning" density="compact">
                {{ $t('results.notFound') }}
              </v-alert>
            </v-expansion-panel-text>
          </v-expansion-panel>
        </v-expansion-panels>
      </v-col>
    </v-row>
  </div>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import type { ApiMatchEvaluation } from '@source-taster/types'
import { useExtractionStore } from '@/stores/extraction'
import { useVerificationStore } from '@/stores/verification'
import { cslToBibtex } from '@/utils/bibtex'
import ScoreBadge from '@/components/ScoreBadge.vue'

const extraction = useExtractionStore()
const verification = useVerificationStore()

const matches = computed<Record<string, ApiMatchEvaluation[]>>(() => {
  const out: Record<string, ApiMatchEvaluation[]> = {}
  for (const key of Object.keys(matchesByRef)) {
    out[key] = matchesByRef[key]
  }
  return out
})

const matchesByRef = computed<Record<string, ApiMatchEvaluation[]>>(() => {
  const out: Record<string, ApiMatchEvaluation[]> = {}
  for (const ref of extraction.references) {
    const key = bestKey(ref.id)
    if (key) out[ref.id] = (bestByRef.value[ref.id] ?? [])
  }
  return out
})

function bestKey(id: string): string | null {
  return null // ersetzt in Step 8 durch echte Best-Match-Logik
}
</script>
```

Hinweis: Die obige Draft-Logik für "matches" ist provisorisch und wird in **Step 8** durch eine saubere Implementierung ersetzt (Store hält zusätzlich `bestEvaluations: Record<refId, ApiMatchEvaluation[]>`). Führe Step 8 direkt nach Step 7 aus.

- [ ] **Step 8: Verification-Store um bestEvaluations erweitern (ersetze provisorische Logic)**

In `src/stores/verification.ts` state ergänzen:

```ts
bestEvaluations: {} as Record<string, ApiMatchEvaluation[]>,
```

und in `verifyAll` nach `const bestScore = bestScoreOf(data.evaluations)`:

```ts
this.bestEvaluations[reference.id] = data.evaluations
```

In `ResultsView.vue` das Script-`computed` ersetzen durch:

```ts
const bestMatches = computed<Record<string, ApiMatchEvaluation[]>>(() => verification.bestEvaluations)
```

und im Template `matches` → `bestMatches` verwenden. Dann `matches`/`matchesByRef`/`bestKey`-Blöcke löschen.

Run: `pnpm --filter @source-taster/web typecheck` → PASS

- [ ] **Step 9: ScoreBadge.vue + RefCard-Komponenten schreiben**

`src/components/ScoreBadge.vue`:

```vue
<template>
  <v-chip :color="color" text-color="white" size="small" variant="flat">
    {{ label }}
  </v-chip>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import type { VerificationStatus } from '@/utils/scores'

const props = defineProps<{ status: VerificationStatus; score: number | null }>()

const color = computed(() => {
  if (props.status === 'verified') return 'success'
  if (props.status === 'partial') return 'accent'
  return 'error'
})

const label = computed(() => {
  if (props.status === 'verified') return `${props.score}% · Verified`
  if (props.status === 'partial') return `${props.score}% · Partial`
  return 'Not found'
})
</script>
```

`src/components/RefCard.vue`: Karte für eine Referenz (Titel, Originaltext, ScoreBadge-Slot) — Verwendung in ResultsView optional; einfache Variante:

```vue
<template>
  <v-card class="mb-2" variant="outlined">
    <v-card-text>
      <div class="d-flex align-center">
        <span class="text-subtitle-1">{{ title }}</span>
        <v-spacer />
        <slot name="badge" />
      </div>
      <p class="text-body-2 text-medium-emphasis mt-2">{{ originalText }}</p>
    </v-card-text>
  </v-card>
</template>

<script setup lang="ts">
import { computed } from 'vue'
const props = defineProps<{ title: string; originalText: string }>()
const title = computed(() => props.title || props.originalText.slice(0, 80))
</script>
```

- [ ] **Step 10: AboutView.vue schreiben (ersetzt Dummy)**

Statischer Text: was die Web-Version kann, Verweis auf Extension (Chrome Web Store Link), Privacy-Hinweis (Keys verschlüsselt serverseitig), Links zur Landing + Docs.

```vue
<template>
  <v-row justify="center">
    <v-col cols="12" md="8">
      <h1 class="text-h4 mb-4">{{ $t('about.title') }}</h1>
      <p class="text-body-1 mb-3">{{ $t('about.webIntro') }}</p>
      <p class="text-body-1 mb-3">{{ $t('about.extensionIntro') }}</p>
      <v-btn color="primary" href="https://chromewebstore.google.com/detail/the-source-taster/leggmjghcbdfilhfkgnllhnhhbalpanp" target="_blank" rel="noopener">
        {{ $t('about.extensionLink') }}
      </v-btn>
    </v-col>
  </v-row>
</template>

<script setup lang="ts">
</script>
```

- [ ] **Step 11: i18n-Locales für neue Keys erweitern**

`src/locales/en.json`:

```json
{
  "app": { "title": "Source Taster", "tagline": "Verify your references" },
  "nav": { "about": "About", "results": "Results" },
  "home": {
    "title": "Verify your references",
    "subtitle": "Paste a reference list or upload a PDF. We extract the citations, search 5 academic databases, and score every match.",
    "textareaLabel": "Paste references here",
    "pdfLabel": "Or upload a PDF",
    "useAiLabel": "Use AI extraction (requires API key)",
    "extractButton": "Extract references"
  },
  "results": {
    "back": "Back",
    "verifyButton": "Verify all",
    "export": "Export",
    "empty": "No references extracted yet — go back and paste text or upload a PDF.",
    "match": "Best match",
    "notFound": "No matching source found. This reference could be hallucinated — double-check it!",
    "verified": "Verified",
    "partial": "Partial match",
    "notFoundShort": "Not found"
  },
  "about": {
    "title": "About Source Taster Web",
    "webIntro": "This web app verifies references online — no installation needed. Paste text or upload a PDF, we extract and check every reference against 5 academic databases.",
    "extensionIntro": "For checking references directly in your workflow (context menu, sidepanel), install the browser extension.",
    "extensionLink": "Get the extension (Chrome Web Store)"
  }
}
```

`src/locales/de.json` — gleiche Keys, deutsche Texte:

```json
{
  "app": { "title": "Source Taster", "tagline": "Prüfe deine Referenzen" },
  "nav": { "about": "Über", "results": "Ergebnisse" },
  "home": {
    "title": "Prüfe deine Referenzen",
    "subtitle": "Füge eine Referenzliste ein oder lade ein PDF hoch. Wir extrahieren die Zitate, durchsuchen 5 akademische Datenbanken und bewerten jeden Treffer.",
    "textareaLabel": "Referenzen hier einfügen",
    "pdfLabel": "Oder PDF hochladen",
    "useAiLabel": "KI-Extraktion verwenden (erfordert API-Key)",
    "extractButton": "Referenzen extrahieren"
  },
  "results": {
    "back": "Zurück",
    "verifyButton": "Alle prüfen",
    "export": "Export",
    "empty": "Noch keine Referenzen extrahiert — gehe zurück und füge Text ein oder lade ein PDF hoch.",
    "match": "Bester Treffer",
    "notFound": "Keine Quelle gefunden. Diese Referenz könnte halluziniert sein — prüfe sie noch einmal!",
    "verified": "Verifiziert",
    "partial": "Teilweise übereinstimmend",
    "notFoundShort": "Nicht gefunden"
  },
  "about": {
    "title": "Über Source Taster Web",
    "webIntro": "Diese Web-App prüft Referenzen online — ganz ohne Installation. Füge Text ein oder lade ein PDF hoch; wir extrahieren und prüfen jede Referenz gegen 5 akademische Datenbanken.",
    "extensionIntro": "Für die Prüfung direkt im Arbeitsfluss (Kontextmenü, Sidepanel) installiere die Browser-Erweiterung.",
    "extensionLink": "Erweiterung holen (Chrome Web Store)"
  }
}
```

- [ ] **Step 12: Full-Check**

Run: `pnpm --filter @source-taster/web test` → PASS
Run: `pnpm --filter @source-taster/web typecheck` → PASS
Run: `pnpm --filter @source-taster/web build` → PASS
Run: `pnpm --filter @source-taster/web lint` → PASS

- [ ] **Step 13: Commit**

```bash
git add apps/web/src
git commit -m "feat(web): home, results and about views with export"
```

---

### Task 6: Theme-Polish, App-Shell und README/ROADMAP-Update

**Files:**
- Modify: `apps/web/src/App.vue` (Brand-Shell, Theme-Toggle light/dark, Footer, i18n-Titel)
- Modify: `apps/web/src/main.ts` (falls nötig)
- Modify: `README.md` (Web-App-Link unter "Quick Install")
- Modify: `ROADMAP.md` (Phase 1.3: "Web Workspace" Checkliste)

**Interfaces:**
- Consumes: Locale-Keys `app.*`, `nav.*` aus Task 5
- Produces: — (nur UI-Verbesserungen + Doku)

- [ ] **Step 1: App.vue polieren**

Header: Logo (mdi oder SVG), Titel aus `$t('app.title')`, Nav `Home`/`Results`/`About`, Theme-Toggle (`v-btn icon @click="toggleTheme"` mit `useTheme()`), Footer mit Brand-Farben + Privacy-Hinweis. Kein Kommentar-Code; Vuetify-idiomatisch.

```vue
<template>
  <v-app>
    <v-app-bar color="primary" dark flat>
      <v-app-bar-title>
        <router-link to="/" class="text-white text-decoration-none d-flex align-center ga-2">
          <v-icon icon="mdi-book-search" />
          {{ $t('app.title') }}
        </router-link>
      </v-app-bar-title>
      <v-btn to="/" text>{{ $t('nav.home') }}</v-btn>
      <v-btn to="/results" text>{{ $t('nav.results') }}</v-btn>
      <v-btn to="/about" text>{{ $t('nav.about') }}</v-btn>
      <v-btn icon @click="toggleTheme" :title="themeLabel">
        <v-icon>{{ isDark ? 'mdi-white-balance-sunny' : 'mdi-weather-night' }}</v-icon>
      </v-btn>
    </v-app-bar>

    <v-main>
      <v-container fluid class="pa-6">
        <router-view />
      </v-container>
    </v-main>

    <v-footer app color="primary-dark" dark>
      <v-col class="text-center">
        <a href="https://sourcetaster.com" class="text-white">sourcetaster.com</a> ·
        <a href="https://github.com/jeffnawroth/source-taster" class="text-white">GitHub</a> ·
        {{ $t('app.tagline') }}
      </v-col>
    </v-footer>
  </v-app>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import { useTheme } from 'vuetify'

const theme = useTheme()
const isDark = computed(() => theme.global.current.value.dark)
const themeLabel = computed(() => (isDark.value ? 'Light mode' : 'Dark mode'))

function toggleTheme() {
  theme.global.name.value = isDark.value ? 'light' : 'dark'
}
</script>
```

Locale-Keys `nav.home` ergänzen (`en`: "Home", `de`: "Start") in beiden JSONs.

- [ ] **Step 2: README aktualisieren**

In der Tabelle "Quick Install" eine Zeile ergänzen (vor der Chrome-Zeile oder als neue Tabelle "Online"):

```markdown
| Online (ohne Installation)   | [Web App](https://sourcetaster.com/app) — Paste & Verify im Browser |
```

Außerdem im Einleitungstext (`Browser extension + API...`) zu `Browser extension + web app + API` erweitern.

- [ ] **Step 3: ROADMAP aktualisieren**

Unter `### 1.3 Landing Page Erweiterungen` eine Checkbox ergänzen und den Web-Workspace als erledigt/teils erledigt markieren:

```markdown
- [x] Web Workspace (`sourcetaster.com/app`) — Paste/PDF → Extract → Verify → Export
- [ ] Screen-Recording GIF (15s: Paste → Extract → Search → Match)
```

- [ ] **Step 4: Verifikation**

Run: `pnpm --filter @source-taster/web typecheck && pnpm --filter @source-taster/web build` → PASS
Run: `pnpm --filter @source-taster/web lint` → PASS
Run: `grep -R "localhost:8000" apps/web/dist/ || true` → Expected: keine Treffer
Run: `grep -R "api.sourcetaster.com" apps/web/dist/ | head -3` → Expected: Treffer in JS-Bundle

- [ ] **Step 5: Commit**

```bash
git add apps/web README.md ROADMAP.md
git commit -m "feat(web): brand shell, readme and roadmap updates"
```

---

### Task 7: Docker-Deploy (Service `web`)

**Files:**
- Create: `apps/web/Dockerfile`
- Create: `apps/web/nginx.conf`
- Modify: `docker-compose.yml` (Service `web` auf Port 8084)
- Modify: `.github/workflows/release.yml` (deploy-Loop `landing docs api` → `landing docs api web`)
- Modify: `.github/workflows/ci.yml` + `lint.yml` falls sie Apps hardcoden (prüfen! nur ergänzen wenn sie explizite App-Listen enthalten)

**Interfaces:**
- Consumes: `apps/web` Build-Artefakt (`dist/`)
- Produces: Docker-Image `web` (nginx:alpine, Port 80), compose-Service `web` (Host-Port 8084), Release-Deploy inkludiert `web`.

- [ ] **Step 1: Dockerfile schreiben**

`apps/web/Dockerfile` (Muster: apps/landing/Dockerfile — erst lesen! `apps/landing/Dockerfile` als Vorlage verwenden):

```dockerfile
FROM node:22-alpine AS build
WORKDIR /app
RUN corepack enable
COPY package.json pnpm-lock.yaml pnpm-workspace.yaml ./
COPY packages/types ./packages/types
COPY apps/web ./apps/web
RUN pnpm install --frozen-lockfile
RUN pnpm --filter @source-taster/types build
RUN pnpm --filter @source-taster/web build

FROM nginx:alpine
COPY apps/web/nginx.conf /etc/nginx/conf.d/default.conf
COPY --from=build /app/apps/web/dist /usr/share/nginx/html
EXPOSE 80
```

WICHTIG: Vor dem Schreiben `apps/landing/Dockerfile` lesen und dessen Muster (Build-Kontext, Lockfile-Handling) übernehmen — der Compose-Build-Kontext ist die Repo-Root (siehe `docker-compose.yml` → `context: .`, `dockerfile: apps/landing/Dockerfile`).

- [ ] **Step 2: nginx.conf schreiben**

```nginx
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

(Hash-Routing macht Rewrites überflüssig; `try_files` fängt Root-Aufrufe.)

- [ ] **Step 3: docker-compose.yml Service ergänzen**

Nach dem `landing`-Service block einfügen:

```yaml
  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    ports:
      - '8084:80'
    restart: unless-stopped
    healthcheck:
      test: [CMD, curl, -f, 'http://localhost:80/']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
```

- [ ] **Step 4: release.yml deploy-Loop erweitern**

Zeile `for service in landing docs api; do` → `for service in landing docs api web; do`

- [ ] **Step 5: CI/Lint-Workflows prüfen und ggf. erweitern**

`ci.yml`/`lint.yml` lesen. Wenn sie `pnpm build`/`pnpm lint`/`pnpm run -r` nutzen, ist nichts zu tun. Wenn sie explizite Pakete nennen (z. B. `pnpm --filter @source-taster/extension build`), `@source-taster/web` ergänzen.

- [ ] **Step 6: Lokaler Docker-Build verifizieren**

Run: `docker compose build web`
Expected: Image baut erfolgreich.

- [ ] **Step 7: Commit**

```bash
git add apps/web/Dockerfile apps/web/nginx.conf docker-compose.yml .github/workflows/release.yml
git commit -m "feat(web): docker service for web workspace"
```

---

### Task 8: E2E-Verifikation gegen lokale API

**Files:** (keine dauerhaften Dateien; temporär manuell)

**Interfaces:**
- Consumes: laufende lokale API (`docker compose up -d anystyle api`) + Vite-Dev-Server
- Produces: Verifikationsnachweis (Log)

- [ ] **Step 1: API + Anystyle hochfahren**

Run: `docker compose up -d anystyle api` und `curl -s http://localhost:8000/health`
Expected: `{"status":"ok"}` (oder entsprechendes Healthy-Signal)

- [ ] **Step 2: API-Flow manuell testen (ohne UI)**

Mit einer echten Beispiel-Referenz (z. B. LeCun 2015 Nature):

```bash
curl -s -X POST http://localhost:8000/api/extract \
  -H 'Content-Type: application/json' -H 'X-Client-Id: 11111111-1111-4111-8111-111111111111' \
  -d '{"text":"LeCun, Y., Bengio, Y., & Hinton, G. (2015). Deep learning. Nature, 521(7553), 436-444."}'
```

Expected: `success: true` mit 1 Referenz. Danach Search + Match analog (aus dem Extract-Ergebnis die id+metadata nehmen). Bei Fehlern: Debugging (systematic-debugging anwenden).

- [ ] **Step 3: Dev-Server + UI-Smoke-Test**

Run: `pnpm --filter @source-taster/web dev` (Port 5173, Proxy auf 8000)
Browser-Handtest (oder curl auf `http://localhost:5173/`): App lädt, kein JS-Fehler in der Konsole (falls Browser verfügbar; sonst Build-Beweis aus Task 6 genügt + API-Test aus Step 2).

- [ ] **Step 4: Root-Scripts-Verifikation**

Run: `pnpm build` (alle Apps) → PASS
Run: `pnpm typecheck` → PASS
Run: `pnpm test` → PASS
Run: `pnpm lint` → PASS

- [ ] **Step 5: Commit (falls Fixes entstanden; sonst entfällt)**

```bash
git add -A && git commit -m "fix(web): e2e verification fixes"
```

---

## Self-Review-Ergebnis

- **Spec-Abdeckung**: Kern-Workflow (Task 3–5), Brand (Task 6), Deploy (Task 7), Verifikation (Task 8) — alle Spec-Abschnitte abgedeckt. Nicht-Scope (Accounts, Rate-Limits, Integrationen) bewusst außen vor.
- **Placeholder-Scan**: Keine TBDs; alle Codeblöcke vollständig.
- **Typkonsistenz**: `apiClient` gibt `Promise<unknown>` (data-Envelope) zurück — Services casten explizit. `classifyScore`/`bestScoreOf` in Task 4 (scores.ts) vor den Stores definiert. `verification.bestEvaluations` wird in Task 5 Step 8 ergänzt und von ResultsView konsumiert — Reihenfolge eingehalten.

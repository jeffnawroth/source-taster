/**
 * Generic mode types for controlling AI behavior
 * Used across extraction and matching domains
 */
export enum Mode {
  STRICT = 'strict',
  BALANCED = 'balanced',
  TOLERANT = 'tolerant',
  CUSTOM = 'custom',
}

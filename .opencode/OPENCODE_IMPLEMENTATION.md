# OpenCode Implementation Pointer

The canonical OpenCode adapter is
`docs/ai-os/runtimes/opencode/implementation.md`. It implements the
runtime-neutral CORE in `docs/ai-os/core/` and records technical versus
instruction-level guarantees. This pointer intentionally contains no duplicate
runtime policy.
